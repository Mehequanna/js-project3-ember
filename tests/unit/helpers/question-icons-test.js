
import { questionIcons } from 'ember-questions/helpers/question-icons';
import { module, test } from 'qunit';

module('Unit | Helper | question icons');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionIcons([42]);
  assert.ok(result);
});

